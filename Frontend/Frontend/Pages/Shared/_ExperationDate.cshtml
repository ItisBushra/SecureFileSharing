<div class="modal fade" id="calendarModal" tabindex="-1" aria-labelledby="calendarModalLabel" aria-hidden="true"
     data-bs-backdrop="static" data-bs-keyboard="false">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-body">
                <p>
                    After saving a date, the encrypted file will remain accessible until that specified time.
                </p>

                <strong> Please note:</strong> <p>
                    Unless explicitly selected below, the encrypted data will remain in our system even after the expiration date.
                </p>

                <div class="container">
                    <div class="row">
                        <div class='col-sm-12'>
                            <input type='text' class="form-control" id='datetimepicker4' placeholder="Select date and time">
                        </div>
                    </div>
                </div>
                <div class="mt-3" style="font-size: 0.9rem;">
                    Only share the password with the recipient.
                        If it is lost, the file cannot be decrypted.
                    </div>
                <div class="container">

                    <div class="row">
                        <div class='col-sm-12 mt-2'>
                           
                            <input type='text' name="password" class="form-control" value="" id='password' placeholder="Set a Password" required>
                        </div>
                    </div>
                </div>
                <div class="form-check mt-3">
                    <input class="form-check-input" type="checkbox" value="" id="flexCheckDefault">
                    <label class="form-check-label" for="flexCheckDefault">
                        Automatically delete the file once the expiration date is reached.
                    </label>
                </div>
            </div>
            <div class="modal-footer d-flex justify-content-between w-100">
            <button id="saveDateBtn" type="button" class="btn btn-outline-success">Save</button>
            <a id="noExpirationBtn" href="#" class="text-muted">I don't need an expiration date</a>
        </div>
        </div>
    </div>
</div>